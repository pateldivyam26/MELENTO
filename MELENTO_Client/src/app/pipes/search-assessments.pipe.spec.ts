import { SearchAssessmentsPipe } from './search-assessments.pipe';

describe('SearchAssessmentsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAssessmentsPipe();
    expect(pipe).toBeTruthy();
  });
});
